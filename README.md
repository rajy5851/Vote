# devdocs

## setup

```bash
# 프로젝트 디렉토리 생성
npx create-react-app client

# git 설정
git init

# github에 vote 레파지토리를 생성

git add .

git commit -m "first commit"

git remote add origin https://github.com/ganzik/vote.git

git push -f -u origin master
```
